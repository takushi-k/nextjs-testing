version: 0.2
phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  base-directory: .next
  files:
    - '**/*'
cache:
  paths:
    - node_modules/**/*
